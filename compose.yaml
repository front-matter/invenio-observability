# Docker Compose configuration for InvenioRDM observability stack
# configuration files are stored under /data/coolify/services

version: '3.9'
services:
  loki:
    image: 'grafana/loki:2.9.17'
    container_name: loki
    command: '-config.file=/etc/loki/loki-config.yaml'
    networks:
      - coolify
    volumes:
      - '/data/coolify/services/loki/loki-config.yaml:/etc/loki/loki-config.yaml:ro'
      - '/data/coolify/services/loki/loki-data:/loki'
    ports:
      - '3100:3100'
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - 'wget --no-verbose --tries=1 --spider http://localhost:3100/ready || exit 1'
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
  grafana:
    image: 'grafana/grafana:12.4.0'
    container_name: grafana
    environment:
      # use Postgres backend
      - GF_DATABASE_TYPE=postgres
      - GF_DATABASE_NAME=grafana
      - 'GF_DATABASE_HOST=${GF_DATABASE_HOST:-}'
      - 'GF_DATABASE_USER=${GF_DATABASE_USER:-}'
      - 'GF_DATABASE_PASSWORD=${GF_DATABASE_PASSWORD:-}'

      # enable SMTP for notifications
      - GF_SMTP_ENABLED=${GF_SMTP_ENABLED:-true}
      - 'GF_SMTP_HOST=${GF_SMTP_HOST:-}'
      - 'GF_SMTP_USER=${GF_SMTP_USER:-}'
      - 'GF_SMTP_PASSWORD=${GF_SMTP_PASSWORD:-}'
      - 'GF_SMTP_FROM_ADDRESS=${GF_SMTP_FROM_ADDRESS:-}'
      - 'GF_SMTP_FROM_NAME=${GF_SMTP_FROM_NAME:-}'

      # enable local login for admin user and basic auth   
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER:-}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD:-}
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_AUTH_BASIC_ENABLED=false
      - GF_AUTH_ANONYMOUS_ENABLED=false

      # enable login via Keycloak OIDC and disable local login form & basic auth
      - GF_AUTH_GENERIC_OAUTH_ENABLED=${GF_AUTH_GENERIC_OAUTH_ENABLED:-true}
      - GF_AUTH_GENERIC_OAUTH_SCOPES=openid,email,profile
      - GF_AUTH_GENERIC_OAUTH_NAME=Keycloak
      - GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP=true
      - GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH=Admin

      # Grafana 12 Drilldown apps are shipped as plugins.
      - GF_PLUGINS_PREINSTALL=grafana-lokiexplore-app,grafana-metricsdrilldown-app

      # Provide KEYCLOAK_PROVIDER_DOMAIN without a trailing slash, e.g. https://sso.example.org

      - KEYCLOAK_PROVIDER_DOMAIN=${KEYCLOAK_PROVIDER_DOMAIN:-}
      - KEYCLOAK_REALM_NAME=${KEYCLOAK_REALM_NAME:-}
      - 'GF_AUTH_GENERIC_OAUTH_CLIENT_ID=${GF_AUTH_GENERIC_OAUTH_CLIENT_ID:-}'
      - 'GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET=${GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET:-}'
      - 'GF_AUTH_GENERIC_OAUTH_AUTH_URL=${KEYCLOAK_PROVIDER_DOMAIN}/realms/${KEYCLOAK_REALM_NAME}/protocol/openid-connect/auth'
      - 'GF_AUTH_GENERIC_OAUTH_TOKEN_URL=${KEYCLOAK_PROVIDER_DOMAIN}/realms/${KEYCLOAK_REALM_NAME}/protocol/openid-connect/token'
      - 'GF_AUTH_GENERIC_OAUTH_API_URL=${KEYCLOAK_PROVIDER_DOMAIN}/realms/${KEYCLOAK_REALM_NAME}/protocol/openid-connect/userinfo'
      - GF_SERVER_ROOT_URL=${GF_SERVER_ROOT_URL:-}
    volumes:
      - '/data/coolify/services/grafana/grafana-data:/var/lib/grafana'
    networks:
      - coolify
    ports:
      - '3000:3000'
    restart: unless-stopped
    depends_on:
      - loki
    healthcheck:
      test:
        - CMD-SHELL
        - 'wget --spider --quiet http://localhost:3000/api/health || exit 1'
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
  vector:
    image: 'timberio/vector:0.53.0-alpine'
    container_name: vector
    volumes:
      - '/data/coolify/services/vector/vector.toml:/etc/vector/vector.toml:ro'
      - '/var/run/docker.sock:/var/run/docker.sock'
    networks:
      - coolify
    depends_on:
      - loki
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - 'vector validate --no-environment /etc/vector/vector.toml || exit 1'
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s
volumes:
  loki-data: null
  grafana-data: null
  vector-config: null
networks:
  coolify:
    external: true
